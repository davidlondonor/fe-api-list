const TextArea = () => {
	return (
		<div className="textarea-container">
			<textarea
				className="textarea"
				placeholder="Write your message here..."
			/>
		</div>
	);
};

export default TextArea;
